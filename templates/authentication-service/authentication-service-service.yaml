apiVersion: v1
kind: Service
metadata:
  name: authentication-service
  namespace: {{ .Values.global.namespace }}
spec:
  type: ClusterIP
  selector:
    app: authentication-service
  ports:
    - name: http
      port: 8082
      targetPort: 8082
