apiVersion: v1
kind: Service
metadata:
  name: authentication-service-db
  namespace: {{ .Values.global.namespace }}
  labels:
    app: authentication-service-db
spec:
  type: ClusterIP
  selector:
    app: authentication-service-db
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432